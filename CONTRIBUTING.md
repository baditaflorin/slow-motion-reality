# Contributing

Use Node.js 22 or newer.

1. Run `npm install`.
2. Run `make install-hooks`.
3. Use Conventional Commits for every commit.
4. Run `make test`, `make build`, and `make smoke` before pushing.

This is a Mode A GitHub Pages app. Do not add runtime secrets, auth, or a backend without first adding an ADR.
